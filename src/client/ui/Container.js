import { ui } from '/client/context';import App from './pages/<App>/App';import Dashboard from './pages/<Dashboard>/Dashboard';const { Menu } = ui.fragments;export default ui.hoc({  classes: {    wrapper: `      position: relative;      height: 100vh;      width: 100%;    `,    menu: `      position: absolute;      top: 12px;      left: 12px;      cursor: pointer;      z-index: 10;      user-select: none;    `,    pages: `      position: absolute;      top: 0; left: 0; right: 0; bottom: 0;      overflow: hidden;    `,  },  render({ classes }) {    return ui.html`      <div id='pages-wrapper' class=${classes.wrapper}>        <header id='menu' class=${classes.menu}>          ${Menu()}        </header>        <main id='pages' class=${classes.pages}>          ${App()}          ${Dashboard()}        </main>      </div>    `;  }});